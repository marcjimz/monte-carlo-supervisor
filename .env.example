# Databricks CLI profile (required)
# Run: databricks configure --profile my-workspace
DATABRICKS_PROFILE=my-workspace

# Override bundle defaults (optional)
# UC_CATALOG=my_catalog
# UC_SCHEMA=my_schema
